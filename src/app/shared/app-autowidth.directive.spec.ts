import { AppAutowidthDirective } from './app-autowidth.directive';

describe('AppAutowidthDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAutowidthDirective();
    expect(directive).toBeTruthy();
  });
});
